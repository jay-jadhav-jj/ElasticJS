import { Platform, Scene, loadCustomScene } from "../script.js";

// let scene = new Scene(floorHeight, speed, jumpForce, gravity, spawnX = 0, spawnY = 0);

// scene.addPlatform(new Platform(color, x, y, width, height, collision = true)); ...

// loadCustomScene(scene, 4);